package aptms.enums;

/** What kind of leg/component an itinerary item within a TOUR_PACKAGE represents. */
public enum PackageItemType {
    TRANSPORT,
    HOTEL,
    ACTIVITY,
    MEAL,
    OTHER
}
