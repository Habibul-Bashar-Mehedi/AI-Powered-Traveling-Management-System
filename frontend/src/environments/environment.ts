export const environment = {
  production: false,
  apiUrl: '/api',
  apiVersion: 'v1',
  // Origin that uploaded assets (e.g. vendor service images) are served from.
  // Empty string = same-origin (dev proxy forwards /uploads to the backend).
  assetOrigin: '',
};
